# 公众号 Markdown → X 文章 隐私政策

生效日期：2026 年 9 月 23 日

## 扩展用途

本扩展在用户主动操作时，将当前微信公众号文章保存为本地 Markdown 和图片文件；用户随后可选择本地 Markdown 文件夹，在 X Articles 编辑器中导入成自己的文章草稿。

## 访问与处理的数据

在公众号文章页，扩展读取当前文章的标题、正文、图片地址、封面、作者、账号、日期和原文链接。点击下载后，扩展请求该文章引用的图片，并将 Markdown 与图片写入浏览器下载目录。

在 X 文章页，扩展只读取用户主动选择或拖入的 Markdown 与图片文件，解析标题、正文、封面和图片。用户点击“导入到 X 草稿”后，扩展操作当前 X Articles 编辑器，将内容写入用户的 X 草稿。扩展也会检查当前页面是否为 X 文章编辑器，以及当前草稿是否已有内容。

文章和 Markdown 文件可能包含用户或第三方自行写入的个人信息。扩展仅为上述转换与导入功能处理这些内容。

## 处理位置、保留与删除

解析和文件读取在用户浏览器内完成；扩展没有开发者服务器，也不将文章或文件上传给开发者。扩展不使用 `chrome.storage` 保存文章内容，也没有分析或广告 SDK。

下载的 Markdown 与图片保存在用户的下载目录，直到用户自行删除。导入过程中内容会进入 X 编辑器并由 X 保存为用户草稿；草稿的保留与删除由用户在 X 中管理。扩展面板与页面内存中的临时数据会在页面关闭或重新加载后消失。

## 网络请求与第三方

为了下载公众号图片，扩展会从原文引用的图片地址请求数据，通常是微信图片服务。导入含网络图片的 Markdown 时，扩展也可能请求其中的图片地址。图片服务会接收正常的网络请求，适用其自身的隐私规则。

用户点击导入后，文章内容、封面和图片通过 X Articles 编辑器发送给 X，用于生成用户自己的草稿。扩展不会把内容发送到其他开发者控制的服务，也不会自动发布文章。

扩展不出售数据，不用于广告定向或分析，也不向开发者提供人工查看文章的渠道。

## 权限与网站访问

| 权限或范围 | 用途 |
|---|---|
| `downloads` | 按用户操作把 Markdown 与图片保存到本地下载目录，并显示已下载文件。 |
| `https://mp.weixin.qq.com/*` | 在公众号文章页提取用户当前文章内容。 |
| `https://mmbiz.qpic.cn/*`、`https://mmbiz.qlogo.cn/*` | 在文章页直接请求失败时，读取公众号原图或封面。 |
| `https://x.com/*`、`https://twitter.com/*` | 在 X 文章编辑器显示导入面板、读取用户选定的文件并写入草稿。 |

## 代码与安全

所有可执行脚本和 Markdown 解析库都包含在扩展安装包中。扩展不会下载并执行远程代码。选择的本地文件仅在用户操作后被读取；扩展不会扫描其他文件夹。

扩展对通过 Chrome API 获得的信息的使用遵守 Chrome 网上应用店用户数据政策与 Limited Use 要求，仅用于上述用户可见的导出与导入流程。

## 政策变更

如数据处理方式发生重要变化，本政策会更新生效日期与说明。

## 联系方式

隐私问题请发至 [wzfh520@gmail.com](mailto:wzfh520@gmail.com)。
